export async function onRequestGet({env}){try{await env.DB.prepare(`CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT,email TEXT,role TEXT,pin TEXT,title TEXT,active INTEGER DEFAULT 1)`).run();const cols=await env.DB.prepare('PRAGMA table_info(users)').all();if(!(cols.results||[]).some(c=>c.name==='title'))await env.DB.prepare('ALTER TABLE users ADD COLUMN title TEXT').run();await env.DB.prepare(`UPDATE users SET role='senior_engineer', title='Senior Engineer' WHERE lower(name)=lower('Mark Fuller')`).run();const rows=await env.DB.prepare('SELECT id,name,email,role,pin,title,active FROM users ORDER BY name').all();return Response.json({ok:true,users:rows.results||[]})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}