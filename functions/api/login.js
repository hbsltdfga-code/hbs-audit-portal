async function ensure(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT,email TEXT,role TEXT,pin TEXT,title TEXT,active INTEGER DEFAULT 1)`).run();const cols=await env.DB.prepare('PRAGMA table_info(users)').all();const names=(cols.results||[]).map(c=>c.name);if(!names.includes('title'))await env.DB.prepare('ALTER TABLE users ADD COLUMN title TEXT').run();if(!names.includes('active'))await env.DB.prepare('ALTER TABLE users ADD COLUMN active INTEGER DEFAULT 1').run();await env.DB.prepare(`UPDATE users SET active=1 WHERE active IS NULL`).run();await env.DB.prepare(`UPDATE users SET role='manager', title='Manager', active=1 WHERE lower(name) IN (lower('Peter Taylor'),lower('Eward Richards'),lower('Lucy Coppage')) OR lower(email) IN (lower('peter@hbs.local'),lower('eward.richards@hbs.local'),lower('lucy.coppage@hbs.local'))`).run();await env.DB.prepare(`UPDATE users SET role='senior_engineer', title='Senior Engineer', active=1 WHERE lower(name)=lower('Mark Fuller') OR lower(email)=lower('mark.fuller@hbs.local')`).run();await env.DB.prepare(`UPDATE users SET active=0 WHERE lower(name)=lower('Russell Haines')`).run()}const clean=v=>String(v||'').trim().toLowerCase();async function findUser(env,login,pin){const rows=await env.DB.prepare(`SELECT id,name,email,role,pin,title,COALESCE(active,1) AS active FROM users WHERE COALESCE(active,1)=1`).all();return(rows.results||[]).find(u=>{const nm=clean(u.name),em=clean(u.email),p=String(u.pin||'').trim();return p===pin&&(em===login||nm===login||nm.includes(login)||login.includes(nm))})}export async function onRequestPost({request,env}){try{await ensure(env);const b=await request.json();const login=clean(b.email||b.username||''),pin=String(b.pin||b.password||'').trim();if(!login||!pin)return Response.json({ok:false,error:'Enter email/name and PIN.'},{status:400});const user=await findUser(env,login,pin);if(!user)return Response.json({ok:false,error:'Invalid login details.'},{status:401});return Response.json({ok:true,user})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}export async function onRequestGet({request,env}){try{await ensure(env);const u=new URL(request.url);const login=clean(u.searchParams.get('user')||u.searchParams.get('email')||''),pin=String(u.searchParams.get('pin')||'').trim();if(!login||!pin)return Response.json({ok:true,message:'Login API is active'});const user=await findUser(env,login,pin);if(!user)return Response.json({ok:false,error:'Test login failed'},{status:401});return Response.json({ok:true,user})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}