async function ensure(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS toolbox_results (id INTEGER PRIMARY KEY AUTOINCREMENT,engineer_name TEXT,test_type TEXT,level TEXT,score REAL,result TEXT,status TEXT DEFAULT 'Pending Manager Review',answers_json TEXT,manager_name TEXT,manager_notes TEXT,signed_off_at TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`).run()}export async function onRequestGet({request,env}){try{await ensure(env);const u=new URL(request.url),role=u.searchParams.get('role')||'engineer',eng=u.searchParams.get('engineer')||'';let rows;if(role==='manager')rows=await env.DB.prepare('SELECT * FROM toolbox_results ORDER BY id DESC').all();else rows=await env.DB.prepare('SELECT * FROM toolbox_results WHERE lower(engineer_name)=lower(?) ORDER BY id DESC').bind(eng).all();return Response.json({ok:true,results:rows.results||[]})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}export async function onRequestPost({request,env}){try{await ensure(env);const b=await request.json();const ins=await env.DB.prepare(`INSERT INTO toolbox_results (engineer_name,test_type,score,result,status,answers_json,manager_name,manager_notes) VALUES (?,?,?,?,?,?,?,?)`).bind(b.engineer_name||'',b.test_type||'Toolbox Test',Number(b.score||0),b.result||'',b.status||'Pending Manager Review',b.answers_json||'{}',b.manager_name||'',b.manager_notes||'').run();return Response.json({ok:true,id:ins.meta?.last_row_id})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}export async function onRequestPut({request,env}){try{await ensure(env);const b=await request.json();await env.DB.prepare('UPDATE toolbox_results SET status=?, manager_name=?, manager_notes=?, signed_off_at=? WHERE id=?').bind(b.status||'Completed',b.manager_name||'',b.manager_notes||'',new Date().toISOString(),Number(b.id)).run();return Response.json({ok:true,id:b.id})}catch(e){return Response.json({ok:false,error:e.message},{status:500})}}