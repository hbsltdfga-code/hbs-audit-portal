async function loadKnowledge(){setHtml('knowledgeCentre',`<h2>Knowledge Centre</h2><p><button onclick="loadKnowledge()">Refresh</button></p><div class="grid"><label>Search<br><input id="kcSearch"></label><label>Category<br><select id="kcFilter"><option value="">All Categories</option><option>IGEM Standards</option><option>Manufacturer Manuals</option><option>HBS Bulletins</option><option>Gas Safe Guidance</option></select></label></div><p><button onclick="loadKnowledgeRows()">Search</button></p><table><thead><tr><th>Title</th><th>Category</th><th>Reference</th><th>Keywords</th><th>Uploaded</th><th>Action</th></tr></thead><tbody id="kcRows"></tbody></table>`);await loadKnowledgeRows()}
async function loadKnowledgeRows(){try{const q=encodeURIComponent($('kcSearch')?.value||''),cat=encodeURIComponent($('kcFilter')?.value||'');const j=await api(`/api/library?q=${q}&category=${cat}`);$('kcRows').innerHTML=(j.documents||[]).map(d=>`<tr><td>${safe(d.title)}</td><td>${safe(d.category)}</td><td>${safe(d.reference)}</td><td>${safe(d.keywords)}</td><td>${safe(d.created_at)}</td><td><button onclick="openDoc(${d.id})">Open</button></td></tr>`).join('')}catch(e){$('kcRows').innerHTML='<tr><td colspan="6">'+e.message+'</td></tr>'}}
async function openDoc(id){const j=await api('/api/library?id='+id+'&open_by='+encodeURIComponent(HBS.user.name));if(j.document?.file_url)window.open(j.document.file_url,'_blank')}
